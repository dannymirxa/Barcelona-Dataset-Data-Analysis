##run the following command
pip install pandas
pip install numpy
pip install streamlit
pip install pyodbc
pip install matplotlib

##How run the dashboard

1. Make sure the SQL Server Database is available.
2. In command prompt, direct to the folder containing the python scripts.
3. Run the command "streamlit run Dashboard1.py" for Dashboard1, change the command to run another scripts.
