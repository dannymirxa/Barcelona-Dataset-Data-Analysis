##run the following command
pip install pandas
pip install numpy
pip install streamlit
pip install pyodbc
pip install matplotlib

##connection string

the string in cnxn_str needs to be changed according to the SQL Server connection
